# Public origin of the registry API, baked into the SPA at build time (Vite).
# Change this to your endpoint and rebuild the `client` image.
# For local dev, create .env.local (gitignored) with http://localhost:8082
VITE_REGISTRY_URL=https://registry.voltcloud.dev
