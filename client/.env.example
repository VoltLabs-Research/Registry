# Public origin of the registry API, baked into the SPA at build time (Vite).
# Copy to .env (or .env.local for local-only overrides) and set your endpoint.
VITE_REGISTRY_URL=https://registry.voltcloud.dev
